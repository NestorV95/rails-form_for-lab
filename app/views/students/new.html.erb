<h1> New Student </h1> 

<%= form_for :student, (@student) do |f| %>
    <%= f.label student[:first_name] %> 
    <%= f.text_field :first_name %>
    <%= f.label student[:last_name] %> 
    <%= f.text_field :last_name %>
    <%= f.submit "Create Student" %>
<% end %>